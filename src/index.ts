// Use the generated source maps
import "source-map-support/register"

// export { generateWhereClause } from "./graphql-filters"
// export { createSchema } from "./create-graphql-schema"
// export { annotateSchema } from "./annotate-graphql-schema"

import * as R from "ramda"

const atoi = (str: string): number => {
  const zeroCode = "0".charCodeAt(0)
  str.subs
}

console.log(atoi("123"))
console.log(atoi("-123"))
console.log(atoi("-123Extra"))
console.log(atoi("-123.456"))
console.log(atoi("Does not start with a digit"))
